# Global
LOG_LEVEL=info
REQUEST_TIMEOUT_SECONDS=2

# =============================================================================
# SECURITY CONFIGURATION (REQUIRED FOR PRODUCTION)
# =============================================================================
# API Key for Agent Planner authentication
# Generate with: openssl rand -hex 32
# If not set, authentication is DISABLED (dev mode only - INSECURE)
PAGI_API_KEY=

# TLS Configuration (OPTIONAL; advanced)
#
# The Docker Compose profile runs insecure gRPC by default for local dev.
# If you want mTLS for internal gRPC, set these variables for the relevant
# services (see README).
#
# Model Gateway (server-side):
#   TLS_SERVER_CERT_PATH=/app/tls_certs/server.crt
#   TLS_SERVER_KEY_PATH=/app/tls_certs/server.key
#   TLS_CA_CERT_PATH=/app/tls_certs/ca.crt
# Agent Planner (client-side to Model Gateway):
#   TLS_CLIENT_CERT_PATH=/app/tls_certs/client.crt
#   TLS_CLIENT_KEY_PATH=/app/tls_certs/client.key
#   TLS_CA_CERT_PATH=/app/tls_certs/ca.crt

# Ports (legacy bare-metal harness)
PY_AGENT_PORT=8000
RUST_SANDBOX_PORT=8001
GO_BFF_PORT=8002
MEMORY_MOCK_PORT=8003
MEMORY_PORT=8003
MODEL_GATEWAY_GRPC_PORT=50051

# gRPC (Python Agent -> Go Model Gateway)
MODEL_GATEWAY_GRPC_HOST=localhost
MODEL_GATEWAY_GRPC_TIMEOUT_SECONDS=5

# URLs (legacy bare-metal harness defaults)
PY_AGENT_URL=http://localhost:8000
RUST_SANDBOX_URL=http://localhost:8001
GO_BFF_URL=http://localhost:8002
# Memory Service (bare metal default)
MEMORY_URL=http://localhost:8003

# URLs (Docker Compose overrides suggested)
# PY_AGENT_URL=http://pagi-python-agent:8000
# RUST_SANDBOX_URL=http://pagi-rust-sandbox:8001
# GO_BFF_URL=http://pagi-go-bff:8002
# MEMORY_URL=http://pagi-mock-memory:8003

# API keys (placeholders)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Model Gateway LLM provider switching
# - mock: boots with zero secrets; deterministic JSON output (recommended for first run)
# - openrouter: requires OPENROUTER_API_KEY
# - ollama: requires a running Ollama instance (OLLAMA_BASE_URL)
LLM_PROVIDER=mock

# OpenRouter (OpenAI-compatible)
OPENROUTER_API_KEY=
OPENROUTER_MODEL_NAME=mistralai/mistral-7b-instruct:free

# Ollama (OpenAI-compatible; local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL_NAME=llama3
